// Aggregated Drizzle schema. Entity tables are defined under each
// src/entities/<name>/schema.ts and re-exported here so drizzle-kit has a
// single entry point. (Populated in Phase 2 — Entities & Migrations.)
export {};
